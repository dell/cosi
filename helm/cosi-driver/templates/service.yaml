apiVersion: v1
kind: Service
metadata:
  name: {{ include "cosi-driver.fullname" . }}
  labels:
    {{- include "cosi-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cosi-driver.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosi-driver.fullname" . }}-test-connection"
  labels:
    {{- include "cosi-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosi-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
